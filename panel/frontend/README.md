# Panel Frontend

Static frontend served by nginx. It expects the backend to be reachable at the same origin (proxy or same network). For local testing with `docker-compose`, the frontend will be available at `http://localhost:8080` and the backend at `http://localhost:4000`.
